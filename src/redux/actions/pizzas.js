export const setPizzas = (items) => ({
  //action creator

  type: 'SET_PIZZAS',
  payload: items,
});

export const blabla = { type: 'LOL' }; //action

export const setCategory = (catIndex) => ({
  type: 'SET_CATEGORY',
  payload: catIndex,
});
